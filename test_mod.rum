dd => [ 
  rstd: u32,
  make: f32,
  test: u32
] 

rdr => [rstd:u8, make:u16, test: u32]

String => [len:u32, data: u32]

// t () => rdr :[rstd = 2, test = 90]


//red (t:f32) => f32 t + 1



entry (a:?, b:?, c:?) => u32 {
  d: rdr = :[test = a, make = b, rstd = c]
  d.make
  // if d.test is > 2  { d.make } else { if d.make is > 200 { d.rstd } else { d.test } }
} 
