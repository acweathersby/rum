DD :: [ test: f32 ]


TYPE :: [
  length: u32 = 32,
  props : [ u32 ; length ]
]

/*

#allocator 
alloc :: ( element_count: u32, ty: type, state: allocation_state ) : *[ty.type]  {
  if element_count > 1 {
     // Allocating an array
  } otherwise { 
    // Allocating a single object
    
    size = ty.byte_size

    // Perform 


  }
}

TYPE_TABLE :: [
  
]

TYPE_REF :: [
  prim_type_data: u32,
  type_entry_index: u32,
]

TYPE :: [ 
  name             : str,
  const ele_count  : u32,
  ele_byte_size    : u32,
  alignment        : u32,
  const prop_count : u32,
  props            : [TYPE_PROP; prop_count]
]

TYPE_PROP :: [
  name         : u32,
  type         : u32,
  byte_offset  : u32,
  count        : i32
]

struct str :: [ #immut len: u32; bytes: [u8;len] ]

str_type :: [ 
  name          : "str",
  alignment     : 1,
  ele_byte_size : 1,
  ele_count     : 0, // Implies that a length pointer is stored alongside the actual data
  prop_count    : 1,
  props         : [ 
    name:       "char",
    byte_offset: 0,
    type:        u8,
    last:        true
    is_array:    true, 
  ]
]
*/


u32_type :: () : type {
  u32_type = [
    ele_count     : 1,
    ele_byte_size : 4,
    alignment     : 4,
    prop_count    : 0, 
  ]
}